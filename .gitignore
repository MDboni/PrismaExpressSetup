node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Build output — never commit it. A tracked dist/ is how the injected
# "preinstall": "node dist/setup.js" payload was delivered.
dist
